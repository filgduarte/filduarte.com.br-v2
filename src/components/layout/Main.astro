---
import FollowingCursor from '../FollowingCursor.astro';
const { id } = Astro.props;
---

<main id={id}>
    <FollowingCursor />
    <slot />
</main>

<style>
    main {
        margin: 0 auto;
        padding: 0 var(--default-spacing);
    }
</style>